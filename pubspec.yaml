name: salyan_file_manager
description: File manager simples para Android (ARMv7)
publish_to: "none"

version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.0
  permission_handler: ^11.3.0

flutter:
  uses-material-design: true
